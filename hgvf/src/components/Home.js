import React from 'react';
import MapContainer from './GoogleMap'

const Home = (props) => (
    <div className="home">
      <div className="container">
        <MapContainer />
      </div>
    </div>
  )

export default Home